import { UsersSortValidationPipe } from './users-sort-validation.pipe';

describe('UsersSortValidationPipe', () => {
  it('should be defined', () => {
    expect(new UsersSortValidationPipe()).toBeDefined();
  });
});
